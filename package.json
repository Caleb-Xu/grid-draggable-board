{
    "name": "grid-draggable-board",
    "private": true,
    "description": "拖拽面板组件工程",
    "engines": {
        "node": ">=16",
        "pnpm": ">=7"
    },
    "devDependencies": {
        "husky": "^8.0.0",
        "prettier": "^3.0.3",
        "pretty-quick": "^4.0.0"
    },
    "scripts": {
        "preinstall": "npx only-allow pnpm",
        "prepare": "husky install",
        "format": "prettier --write ."
    }
}
